import React from "react";
import ReactDOM from "react-dom/client";
import CashierApp from "./CashierApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CashierApp />
  </React.StrictMode>
);
